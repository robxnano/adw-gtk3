gtk2_dir = join_paths(theme_dir, 'gtk-2.0')

# TODO: ensure all assets are rendered

# Install asset files to themes/ThemeName/gtk-2.0/assets
install_subdir('assets', install_dir : gtk2_dir, strip_directory : false)

install_subdir('rc', install_dir: gtk2_dir, strip_directory : true)
